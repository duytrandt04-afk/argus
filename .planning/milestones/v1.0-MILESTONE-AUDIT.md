---
milestone: v1.0
audited: 2026-05-27T09:35:00Z
status: tech_debt
scores:
  requirements: 60/60 traceability-clean
  phases: 3/3 complete
  integration: 8/8 verified
  flows: 5/5 implementation-verified
gaps:
  requirements: []
  integration: []
  flows: []
open_artifacts:
  - category: "uat"
    phase: "03"
    file: "03-HUMAN-UAT.md"
    status: "partial"
    evidence: "3 pending human scenarios: doctor privacy warning, remote bind startup rejection, end-to-end privacy gate."
  - category: "verification"
    phase: "01"
    file: "01-VERIFICATION.md"
    status: "human_needed"
    evidence: "Clean-machine onboarding timing, GitHub settings/hosted CI confirmation, and migration-failure message quality require human confirmation."
  - category: "verification"
    phase: "03"
    file: "03-VERIFICATION.md"
    status: "human_needed"
    evidence: "Doctor output, remote bind runtime rejection, and privacy gate E2E require human confirmation."
tech_debt:
  - phase: "01-local-adoption-baseline"
    items:
      - "docs/install.md doctor docs mention test/lint checks no longer run by doctor script; phase verification classified this as documentation drift."
  - phase: "03-mature-local-product"
    items:
      - "backend/internal/privacy/ignore/ignore.go had a duplicate loop noted by verification; later 03-REVIEW-FIX.md records related matcher cleanup as fixed."
nyquist:
  overall: "skipped"
  reason: "workflow.nyquist_validation is false in .planning/config.json"
---

# Milestone v1.0 Audit

**Status:** Tech debt  
**Audited:** 2026-05-27  
**Scope:** Phase 1 Local Adoption Baseline, Phase 2 Reliable Daily Use, Phase 3 Mature Local Product  
**Plans:** 19/19 complete  

## Summary

The implementation evidence is strong: all three phases are complete on disk, their plan summaries exist, and phase verification reports show the intended product behavior is implemented. Cross-phase wiring also checks out: version metadata flows from backend to frontend, readiness flows from sqlite migrations into `/readyz`, hook ingestion now gates ignored events before persistence/SSE, exports are protected by `Sec-Fetch-Site`, and CORS/remote-bind policy is wired through config into the router and server startup.

The implementation and requirements traceability are clean enough to archive after explicit acceptance of remaining human close-out checks:

- `REQUIREMENTS.md` was updated on 2026-05-27 so `DIAG-03`, `DIAG-05`, and `REL-05` are checked and marked `Complete`.
- Phase 01 and Phase 03 verification files still require human confirmation.
- Phase 03 human UAT is partial with 3 pending scenarios.

## Phase Readiness

| Phase | Plans | Summaries | Verification Status | Audit Result |
|-------|-------|-----------|---------------------|--------------|
| 1. Local Adoption Baseline | 6/6 | 6/6 | human_needed | Implementation verified; human runtime/platform checks remain |
| 2. Reliable Daily Use | 8/8 | 8/8 | passed | Complete |
| 3. Mature Local Product | 5/5 | 5/5 | human_needed | Implementation verified; human runtime/E2E checks remain |

## Requirements Coverage

| Area | Total | Clean | Gaps |
|------|-------|-------|------|
| Installation & Distribution | 7 | 7 | 0 |
| CI & Verification | 6 | 6 | 0 |
| Diagnostics & Health | 6 | 6 | 0 |
| Data Lifecycle | 7 | 7 | 0 |
| Event Data Model | 5 | 5 | 0 |
| Backend Hardening | 6 | 6 | 0 |
| Security | 5 | 5 | 0 |
| Testing & Regression | 7 | 7 | 0 |
| Release & Versioning | 5 | 5 | 0 |
| Privacy & Data Controls | 3 | 3 | 0 |
| Contributor Infrastructure | 3 | 3 | 0 |

### Unsatisfied / Blocking Items

No code-level unsatisfied requirements were found from phase verification evidence. Remaining items are human-confirmation debt acknowledged at milestone close.

### Traceability Gaps

None. `DIAG-03`, `DIAG-05`, and `REL-05` were updated to checked / `Complete` in `REQUIREMENTS.md` after audit review.

## Open Human Checks

### Phase 01

- Clean-machine onboarding timing: run `./scripts/hooker setup`, `./scripts/hooker doctor`, and first-event quickstart from a clean environment.
- GitHub repository settings and hosted CI: confirm squash-only merge policy and a real push/PR CI run.
- Migration-failure message quality: induce migration failure in a disposable DB and review operator clarity.

### Phase 03

- Doctor privacy warning: run `./scripts/hooker doctor` and confirm it lists prompts, diffs, file paths, tool outputs, raw payloads, and exports.
- Remote bind startup rejection: start with `ADDR=0.0.0.0:8765` and no `HOOKER_ALLOW_REMOTE`; confirm startup exits with the documented actionable error.
- End-to-end privacy gate: create a matching ignore rule, send a matching hook event, confirm zero stored rows and no SSE broadcast.

## Cross-Phase Integration

| Flow | Evidence | Result |
|------|----------|--------|
| Version metadata to UI | `GET /api/version` registered in `NewRouter`; frontend `useVersion` fetches runtime metadata; `VersionBadge` renders in `Sidebar` | Verified |
| Readiness after migrations | sqlite repo exposes `Ready()` after migrations; `main.go` passes `repo.Ready`; `NewRouter` wires `/readyz` to `handler.Readyz(ready)` | Verified |
| Hook ingestion to storage/SSE | `handler.Hook` normalizes payloads; `EventService.AddEvent` persists and broadcasts; degraded path stores raw payload with degraded status | Verified |
| Privacy gate before storage/SSE | `NewRouter` injects matcher; `handler.Hook` checks `MatchEvent` before `SessionModel` and `AddEvent`; tests assert no rows and no SSE broadcast | Verified |
| Export protection | `/api/export/events` and `/api/export/snapshot` are wrapped with `secFetchSite`; export tests cover NDJSON and snapshot paths | Verified |
| CORS policy | `config.Load` derives allowlist; `main.go` passes origins to `server.NewRouter`; `corsAllowlist` echoes only explicit origins and adds `Vary: Origin` | Verified |
| Remote bind policy | `config.AllowRemote` uses `HOOKER_ALLOW_REMOTE=1`; `validateBind` rejects non-loopback addresses unless explicitly allowed; Docker opts in intentionally | Verified |
| Contributor guardrails | `CONTRIBUTING.md` ties adapter changes to fixtures/tests and frontend-backend contract changes to synchronized Go/TypeScript/test updates | Verified |

## Broken Flows

No broken implementation flows found. The remaining flow risk is unexecuted human confirmation for clean-machine onboarding and runtime privacy/security behavior.

## Nyquist Coverage

Nyquist validation is disabled by `.planning/config.json`:

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 01 | skipped | n/a | None while `workflow.nyquist_validation=false` |
| 02 | skipped | n/a | None while `workflow.nyquist_validation=false` |
| 03 | skipped | n/a | None while `workflow.nyquist_validation=false` |

## Recommendation

Before running `$gsd-complete-milestone v1.0`, choose one of these:

1. Explicitly accept the human checks as deferred and let milestone close record the deferred items in `STATE.md`.
2. Insert a closure phase if you want the remaining human runtime checks tracked as executable milestone work rather than deferrals.

## Next Up

Close the gaps inline, then re-run:

```text
$gsd-complete-milestone v1.0
```

Or proceed anyway and accept the listed items as milestone-close debt:

```text
$gsd-complete-milestone v1.0
```
