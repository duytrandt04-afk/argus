---
phase: 05-hook-and-privacy-diagnostics
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/internal/domain/diagnostics.go
  - backend/internal/hookconfig/detector.go
  - backend/internal/service/event_service.go
  - backend/internal/server/router.go
  - backend/cmd/server/main.go
  - backend/tests/internal/hookconfig/detector_test.go
  - backend/tests/internal/service/event_service_test.go
  - backend/tests/internal/handler/diagnostics_test.go
  - backend/tests/internal/server/router_test.go
autonomous: true
requirements: [HOOK-01, HOOK-04, HOOK-05]
user_setup: []
must_haves:
  truths:
    - "D-05: Diagnostics reports best-effort hook config status for Claude Code and Codex only; no Gemini CLI config detection is included in Phase 5."
    - "D-06: Existing Claude/Codex config files without the hooker endpoint report missing."
    - "D-07: Unreadable or invalid config files report unknown with non-sensitive reason codes."
    - "D-08: Hook config detection is implemented in Go with paths and endpoint matching equivalent to scripts/hooker doctor."
    - "D-09: Configured hook status with no observed activity reports a no events warning."
  artifacts:
    - path: "backend/internal/hookconfig/detector.go"
      provides: "Go hook config detector for Claude Code and Codex"
      contains: "Detect"
    - path: "backend/internal/server/router.go"
      provides: "Diagnostics route receives hook config detector inputs"
      contains: "HookConfig"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "backend/internal/server/router.go"
      via: "server.Options"
      pattern: "HookConfig"
---

<objective>
Add Go-based hook config detection for Claude Code and Codex diagnostics.

Purpose: Operators need to know whether the two supported hook configs appear configured, missing, or unknown using doctor-equivalent semantics.
Output: Hook config detector package, diagnostics service wiring, and tests for configured/missing/unknown states.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hook-and-privacy-diagnostics/05-CONTEXT.md
@.planning/phases/05-hook-and-privacy-diagnostics/05-RESEARCH.md
@.planning/phases/05-hook-and-privacy-diagnostics/05-01-SUMMARY.md
@scripts/hooker
@backend/internal/domain/diagnostics.go
@backend/internal/service/event_service.go
@backend/internal/server/router.go
@backend/cmd/server/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hook config detector package</name>
  <files>backend/internal/hookconfig/detector.go, backend/tests/internal/hookconfig/detector_test.go</files>
  <read_first>scripts/hooker, docs/hooks.md, .planning/phases/05-hook-and-privacy-diagnostics/05-CONTEXT.md</read_first>
  <action>Create `backend/internal/hookconfig` with a detector that checks exactly two agents: Claude Code at `~/.claude/settings.json` and Codex at `~/.codex/hooks.json`. D-05: do not add Gemini CLI detection. Accept an injectable home directory and endpoint substring for tests. D-08: use endpoint substring `8765/api/hook` to match current `scripts/hooker doctor` behavior. Return structured results with agent id, config path, status (`configured`, `missing`, `unknown`), and optional non-sensitive reason code. D-06: treat missing file and existing file without endpoint as `missing`. D-07: treat read errors and JSON parse failures as `unknown` with reason codes such as `read_error` and `invalid_json`; do not include file contents or OS error text in diagnostics payload.</action>
  <verify>cd backend && go test ./internal/hookconfig</verify>
  <acceptance_criteria>
    - Tests cover configured Claude and configured Codex.
    - Tests cover missing file and file without endpoint as `missing`.
    - Tests cover invalid JSON and unreadable/read-error path as `unknown` with non-sensitive reason code.
    - Detector has no Gemini CLI entry.
  </acceptance_criteria>
  <done>Hook config detector produces safe, testable Claude/Codex status results.</done>
</task>

<task type="auto">
  <name>Task 2: Wire hook config results into diagnostics composition</name>
  <files>backend/internal/domain/diagnostics.go, backend/internal/service/event_service.go, backend/internal/server/router.go, backend/cmd/server/main.go</files>
  <read_first>backend/internal/domain/diagnostics.go, backend/internal/service/event_service.go, backend/internal/server/router.go, backend/cmd/server/main.go</read_first>
  <action>Extend diagnostics composition so `EventService.Diagnostics` can receive hook config results or a diagnostics options struct. Add fields to `server.Options` rather than widening `NewRouter` positional parameters. In `main.go`, run the hook config detector during startup or route construction and pass the status map into router/service diagnostics. Preserve read-only behavior: diagnostics detects config state but does not patch configs. Merge hook config status into each existing agent row from 05-01. D-09: if hook status is `configured` and no activity exists, keep the `no events` warning. D-06: if hook status is `missing`, expose missing status/warning. D-07: if hook status is `unknown`, expose unknown status/warning without fatal health impact.</action>
  <verify>cd backend && go test ./internal/service ./internal/server ./cmd/server</verify>
  <acceptance_criteria>
    - `server.Options` carries hook config diagnostics input.
    - `main.go` passes detector output without changing setup/doctor shell behavior.
    - Service tests prove `configured`, `missing`, and `unknown` statuses appear on Claude/Codex rows.
    - `health.ready` and handler HTTP 200 behavior are unchanged for hook config warnings.
  </acceptance_criteria>
  <done>Diagnostics rows include best-effort hook config status.</done>
</task>

<task type="auto">
  <name>Task 3: Cover hook config diagnostics at handler/router level</name>
  <files>backend/tests/internal/handler/diagnostics_test.go, backend/tests/internal/server/router_test.go</files>
  <read_first>backend/tests/internal/handler/diagnostics_test.go, backend/tests/internal/server/router_test.go</read_first>
  <action>Update diagnostics handler/router tests to assert hook config status serializes in `agents[]` for Claude Code and Codex. Include cases for configured row with no events warning, missing config, and unknown config reason code. Keep content leak tests focused on captured data and raw config content: response body must not contain config file contents, raw hook command snippets beyond stable status/reason fields, prompts, diffs, tool output, raw payloads, or raw paths from captured events.</action>
  <verify>cd backend && go test ./internal/handler ./internal/server</verify>
  <acceptance_criteria>
    - Handler/router tests prove diagnostics response includes hookConfigStatus for both supported rows.
    - Tests prove unknown config includes only a reason code, not raw error or file content.
    - Tests prove no Gemini row exists.
  </acceptance_criteria>
  <done>Hook config diagnostics are covered through the HTTP response surface.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && go test ./internal/hookconfig`
- [ ] `cd backend && go test ./internal/service ./internal/handler ./internal/server ./cmd/server`
- [ ] Inspect tests or fixture output to confirm only Claude Code and Codex hook config statuses are emitted.
</verification>

<success_criteria>
- All tasks completed.
- Hook config detection is implemented in Go with doctor-equivalent paths and endpoint matching.
- Diagnostics distinguishes `configured`, `missing`, `unknown`, and `no events` without fatal health impact.
- No Gemini CLI config row or detector entry is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hook-and-privacy-diagnostics/05-02-SUMMARY.md`
</output>
