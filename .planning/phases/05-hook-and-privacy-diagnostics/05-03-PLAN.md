---
phase: 05-hook-and-privacy-diagnostics
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - backend/internal/domain/diagnostics.go
  - backend/internal/privacy/ignore/ignore.go
  - backend/internal/service/event_service.go
  - backend/internal/server/router.go
  - backend/cmd/server/main.go
  - backend/tests/internal/privacy/ignore/ignore_test.go
  - backend/tests/internal/service/event_service_test.go
  - backend/tests/internal/handler/diagnostics_test.go
  - backend/tests/internal/server/router_test.go
autonomous: true
requirements: [PRIV-01, PRIV-02, PRIV-03, PRIV-04, HOOK-05]
user_setup: []
must_haves:
  truths:
    - "D-10: Backend reports timestamps/counts only and does not enforce a stale threshold."
    - "D-12: Backend exposes per-agent statuses only and does not compute an overall agent connectivity rollup."
    - "D-13: Diagnostics reports ignore file path, load status, and active pattern count without raw ignore patterns."
    - "D-14: Missing ignore file is safe/OK with zero active rules."
    - "D-15: Diagnostics reports remote-bind posture and CORS local/extra counts without listing full origins."
    - "D-16: Diagnostics includes a static export sensitivity warning covering prompts, diffs, file paths, tool outputs, raw payloads, and exports."
  artifacts:
    - path: "backend/internal/privacy/ignore/ignore.go"
      provides: "Safe ignore matcher status/count support"
      contains: "RuleCount"
    - path: "backend/internal/domain/diagnostics.go"
      provides: "Privacy and security diagnostics response structs"
      contains: "DiagnosticsPrivacy"
    - path: "backend/internal/server/router.go"
      provides: "Config posture passed to diagnostics route"
      contains: "CORSOrigins"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "backend/internal/service/event_service.go"
      via: "diagnostics options passed through server.Options"
      pattern: "IgnorePath"
---

<objective>
Add privacy and security posture diagnostics to the backend contract.

Purpose: Operators need confidence that local privacy controls and network posture are visible without leaking sensitive captured data, raw ignore rules, or full local origin names.
Output: Privacy/security diagnostics structs, ignore status/count support, config posture wiring, and backend tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hook-and-privacy-diagnostics/05-CONTEXT.md
@.planning/phases/05-hook-and-privacy-diagnostics/05-RESEARCH.md
@.planning/phases/05-hook-and-privacy-diagnostics/05-01-SUMMARY.md
@.planning/phases/05-hook-and-privacy-diagnostics/05-02-SUMMARY.md
@docs/privacy.md
@docs/security.md
@backend/internal/config/config.go
@backend/internal/privacy/ignore/ignore.go
@backend/internal/service/event_service.go
@backend/internal/server/router.go
@backend/cmd/server/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add safe ignore-file load status and count</name>
  <files>backend/internal/privacy/ignore/ignore.go, backend/tests/internal/privacy/ignore/ignore_test.go</files>
  <read_first>backend/internal/privacy/ignore/ignore.go, backend/tests/internal/privacy/ignore/ignore_test.go, docs/privacy.md</read_first>
  <action>D-13: add safe status/count support to the ignore package without exposing rule pattern text. Provide either `RuleCount() int` on `Matcher` or a load result helper that returns matcher plus status. Preserve current `Load(path)` behavior for existing callers: missing file returns empty matcher and nil error. D-14: ensure missing file can be represented as load status `missing_ok` or equivalent safe OK status with zero active patterns. Ensure unreadable files still return errors for startup behavior; diagnostics may represent such failures only when intentionally passed a status from tests/options. Do not expose raw pattern strings through exported diagnostics APIs.</action>
  <verify>cd backend && go test ./internal/privacy/ignore</verify>
  <acceptance_criteria>
    - Ignore tests prove comments and blank lines do not count as active patterns.
    - Ignore tests prove missing file results in zero rules and safe OK/missing status.
    - No exported diagnostics-facing method returns raw ignore pattern text.
  </acceptance_criteria>
  <done>Ignore matcher exposes safe count/status data for diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Add privacy/security diagnostics sections and config posture wiring</name>
  <files>backend/internal/domain/diagnostics.go, backend/internal/service/event_service.go, backend/internal/server/router.go, backend/cmd/server/main.go</files>
  <read_first>backend/internal/domain/diagnostics.go, backend/internal/service/event_service.go, backend/internal/server/router.go, backend/cmd/server/main.go, backend/internal/config/config.go, docs/security.md</read_first>
  <action>Add diagnostics response sections for privacy/security posture. D-13: suggested shape is `privacy.ignoreFile` with `path`, `status`, and `activePatternCount`; D-16: `privacy.exportWarning` as the static canonical warning; and `security` or `privacy.network` with remote-bind posture and CORS counts. Pass `Addr`, `AllowRemote`, `IgnorePath`, ignore rule count/status, and `CORSOrigins` through `server.Options` or a diagnostics options struct. D-15: for CORS, expose counts only: total count, local/default count, and extra count. Do not serialize the full origins. Remote-bind posture should indicate loopback vs remote enabled using existing config values and `AllowRemote`; avoid duplicating startup bind validation logic beyond safe display semantics. D-10: do not add stale threshold logic in backend. D-12: do not add an overall agent connectivity rollup.</action>
  <verify>cd backend && go test ./internal/service ./internal/server ./cmd/server</verify>
  <acceptance_criteria>
    - Diagnostics JSON includes ignore file path, status, and active pattern count.
    - Diagnostics JSON includes remote bind posture and CORS counts with no full origin strings.
    - Diagnostics JSON includes a static export sensitivity warning containing prompts, diffs, file paths, tool outputs, raw payloads, and exports.
    - Existing `/healthz`, `/readyz`, CORS, host header, and export routes continue to behave as before.
  </acceptance_criteria>
  <done>Diagnostics exposes privacy/security posture without leaking raw sensitive details.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end backend diagnostics coverage</name>
  <files>backend/tests/internal/service/event_service_test.go, backend/tests/internal/handler/diagnostics_test.go, backend/tests/internal/server/router_test.go</files>
  <read_first>backend/tests/internal/service/event_service_test.go, backend/tests/internal/handler/diagnostics_test.go, backend/tests/internal/server/router_test.go</read_first>
  <action>Add tests covering complete Phase 5 diagnostics output: `version`, `health`, `storage`, `agents`, privacy ignore status/count/path, CORS local/extra counts, remote-bind posture, and export warning. Add negative assertions that the response does not contain raw captured prompt, command, diff/tool output, raw payload, raw ignore pattern text, or full extra CORS origin strings. Include empty-state tests for zero activity plus missing ignore file. Keep tests backend-only; frontend Diagnostics UI belongs to Phase 6.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - Full backend test suite passes.
    - Handler tests prove Phase 5 top-level sections are present.
    - Negative leak tests cover raw captured content, raw ignore patterns, and full CORS origins.
    - Tests do not require human setup or real local config files.
  </acceptance_criteria>
  <done>Phase 5 backend diagnostics contract has complete regression coverage.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && go test ./internal/privacy/ignore`
- [ ] `cd backend && go test ./internal/service ./internal/handler ./internal/server ./cmd/server`
- [ ] `cd backend && go test ./...`
- [ ] Review diagnostics handler tests and confirm frontend files are untouched.
</verification>

<success_criteria>
- All tasks completed.
- Privacy diagnostics expose only path/status/count and never raw ignore patterns.
- Security posture diagnostics expose remote-bind and CORS counts without full origins.
- Export sensitivity warning is stable and canonical.
- Backend test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hook-and-privacy-diagnostics/05-03-SUMMARY.md`
</output>
