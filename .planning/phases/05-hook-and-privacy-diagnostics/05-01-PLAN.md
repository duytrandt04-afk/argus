---
phase: 05-hook-and-privacy-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/domain/diagnostics.go
  - backend/internal/repository/repository.go
  - backend/internal/repository/sqlite/sqlite.go
  - backend/internal/service/event_service.go
  - backend/tests/internal/repository/sqlite/sqlite_test.go
  - backend/tests/internal/service/event_service_test.go
  - backend/tests/internal/handler/diagnostics_test.go
  - backend/tests/internal/server/router_test.go
autonomous: true
requirements: [HOOK-01, HOOK-02, HOOK-03, HOOK-05]
user_setup: []
must_haves:
  truths:
    - "D-01: GET /api/diagnostics includes exactly two supported agent rows: Claude Code and Codex; Gemini CLI is not included in Phase 5."
    - "D-02: Each supported agent row reports session-based eventCount and lastSeenAt where data exists."
    - "D-03: Degraded events attach to Claude Code or Codex only when inferable; non-inferable degraded events remain in an unknown/internal summary."
    - "D-04: Each supported agent row reports latest non-empty normalizerVersion or null."
    - "D-11: Supported-agent degraded activity produces a warning status, but diagnostics health remains non-fatal."
  artifacts:
    - path: "backend/internal/domain/diagnostics.go"
      provides: "Agent diagnostics response structs"
      contains: "DiagnosticsAgent"
    - path: "backend/internal/repository/sqlite/sqlite.go"
      provides: "Targeted SQL aggregate queries for agent diagnostics"
      contains: "DiagnosticsAgentStats"
    - path: "backend/internal/service/event_service.go"
      provides: "Diagnostics agent row composition"
      contains: "claudecode"
  key_links:
    - from: "backend/internal/service/event_service.go"
      to: "backend/internal/repository/repository.go"
      via: "EventRepository diagnostics aggregate method"
      pattern: "DiagnosticsAgentStats"
---

<objective>
Add Phase 5 agent telemetry to the diagnostics backend contract.

Purpose: Operators need to know whether Claude Code and Codex have been observed and whether normalization is healthy, without loading full event lists or exposing captured content.
Output: Diagnostics domain structs, repository aggregate method, service composition, and backend tests for two-agent telemetry rows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hook-and-privacy-diagnostics/05-CONTEXT.md
@.planning/phases/05-hook-and-privacy-diagnostics/05-RESEARCH.md
@.planning/phases/04-diagnostics-data-contract/04-CONTEXT.md
@backend/internal/domain/diagnostics.go
@backend/internal/repository/repository.go
@backend/internal/repository/sqlite/sqlite.go
@backend/internal/service/event_service.go
@backend/tests/internal/repository/sqlite/sqlite_test.go
@backend/tests/internal/service/event_service_test.go
@backend/tests/internal/handler/diagnostics_test.go
@backend/tests/internal/server/router_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend diagnostics domain for agent telemetry</name>
  <files>backend/internal/domain/diagnostics.go</files>
  <read_first>backend/internal/domain/diagnostics.go, .planning/phases/05-hook-and-privacy-diagnostics/05-CONTEXT.md</read_first>
  <action>Add an `Agents []DiagnosticsAgent` field to `Diagnostics` with JSON key `agents`. Define `DiagnosticsAgent` with camelCase JSON fields: `id`, `label`, `eventCount`, `lastSeenAt`, `degradedCount`, `normalizerVersion`, `hookConfigStatus`, `status`, and `warnings`. For this plan, hook config may remain empty or `unknown` until 05-02 fills it. Define repository-facing aggregate structs such as `DiagnosticsAgentStats` with fields for agent id, session count/event count, last seen timestamp, degraded count, and latest normalizer version. Encode "Claude Code" and "Codex" labels in service composition, not in SQL.</action>
  <verify>cd backend && go test ./internal/domain ./internal/service ./internal/repository/sqlite ./internal/handler ./internal/server</verify>
  <acceptance_criteria>
    - `Diagnostics` contains `Agents []DiagnosticsAgent` with json tag `agents`.
    - `DiagnosticsAgent` has camelCase JSON tags and no fields for raw prompts, diffs, tool output, raw payload, path samples, or raw config content.
    - Gemini CLI is not part of the supported-agent constants for Phase 5.
  </acceptance_criteria>
  <done>Diagnostics domain can represent Claude Code and Codex telemetry rows without sensitive content fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add targeted repository aggregates for agent diagnostics</name>
  <files>backend/internal/repository/repository.go, backend/internal/repository/sqlite/sqlite.go, backend/tests/internal/repository/sqlite/sqlite_test.go</files>
  <read_first>backend/internal/repository/repository.go, backend/internal/repository/sqlite/sqlite.go, backend/tests/internal/repository/sqlite/sqlite_test.go</read_first>
  <action>Add `DiagnosticsAgentStats() ([]domain.DiagnosticsAgentStats, error)` to `EventRepository`. Implement it in SQLite with targeted aggregate SQL only. D-02: use `sessions` for supported-agent `eventCount` and `lastSeenAt`: group by `agent`, count sessions or session activity according to existing session rows, and use `MAX(datetime(last_seen_at))` semantics while returning the stored timestamp string. Use `hook_events` only for degraded counts and latest non-empty `normalizer_version`. D-03: for degraded rows with `agent='unknown'`, attach to Claude Code or Codex only if source/transcript data can infer that agent; otherwise leave them out of supported rows for this plan. D-01: do not include Gemini CLI in returned supported rows.</action>
  <verify>cd backend && go test ./internal/repository/sqlite -run 'Diagnostics.*Agent|DiagnosticsStorage'</verify>
  <acceptance_criteria>
    - Repository tests prove empty DB returns no aggregate rows and service later backfills zero rows for Claude/Codex.
    - Repository tests prove Claude Code and Codex session rows produce counts and latest timestamps.
    - Repository tests prove degraded counts and latest normalizer version come from `hook_events`.
    - SQL queries use aggregate functions and do not call `List`, `ListSessions`, or dashboard enrichment flows.
  </acceptance_criteria>
  <done>Repository exposes efficient agent diagnostics aggregates for service composition.</done>
</task>

<task type="auto">
  <name>Task 3: Compose Claude Code and Codex rows in diagnostics service</name>
  <files>backend/internal/service/event_service.go, backend/tests/internal/service/event_service_test.go, backend/tests/internal/handler/diagnostics_test.go, backend/tests/internal/server/router_test.go</files>
  <read_first>backend/internal/service/event_service.go, backend/tests/internal/service/event_service_test.go, backend/tests/internal/handler/diagnostics_test.go, backend/tests/internal/server/router_test.go</read_first>
  <action>Update `EventService.Diagnostics` to call `DiagnosticsAgentStats()` and always return two rows: `claudecode` label `Claude Code`, and `codex` label `Codex`. D-02: use session-based aggregate values where present; set zero counts and `lastSeenAt: null` when absent. D-04: set `normalizerVersion` to latest observed non-empty version or null. Set per-agent `status`/`warnings` for telemetry-only conditions: `no events` warning when no observed activity; `degraded` warning when degraded count is greater than zero; D-11: never change `health.ready` or HTTP status because of agent warnings. Update test doubles to satisfy the expanded repository interface. Update existing handler leak test so `agents` is allowed as a top-level key but forbidden captured strings remain forbidden.</action>
  <verify>cd backend && go test ./internal/service ./internal/handler ./internal/server</verify>
  <acceptance_criteria>
    - Service tests assert exactly two rows and no Gemini row.
    - Service tests assert configured telemetry rows can show `no events` and `degraded` warnings without making health fatal.
    - Handler tests assert diagnostics response includes `agents` and still excludes captured prompt, command, stdout/stderr, raw payload, and path samples.
  </acceptance_criteria>
  <done>GET /api/diagnostics emits safe Claude Code and Codex telemetry rows.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && go test ./internal/repository/sqlite -run 'Diagnostics.*Agent|DiagnosticsStorage'`
- [ ] `cd backend && go test ./internal/service ./internal/handler ./internal/server`
- [ ] Inspect `/api/diagnostics` JSON test fixture/output and confirm `agents` contains `claudecode` and `codex` only.
</verification>

<success_criteria>
- All tasks completed.
- Agent telemetry uses targeted repository aggregates.
- Diagnostics includes Claude Code and Codex rows only.
- No sensitive captured content appears in diagnostics responses.
- Gemini CLI remains deferred per `05-CONTEXT.md` D-01 and D-05.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hook-and-privacy-diagnostics/05-01-SUMMARY.md`
</output>
